# SeleniumJunit5
An example of using Selenium WebDriver and JUnit 5

# create a new repository on the command line
git init

git add README.md 

git commit -m "first commit" 

git branch -M main 

git remote add origin https://github.com/josepharul82/CopilotForAutomation.git 

git push -u origin main 

# push an existing repository from the command line

git remote add origin https://github.com/josepharul82/CopilotForAutomation.git 

git branch -M main 

git push -u origin main 

# application to test
https://axafrance.github.io/webengine-dotnet/demo/home-insurance/


